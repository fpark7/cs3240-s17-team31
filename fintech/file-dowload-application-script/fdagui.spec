# -*- mode: python -*-

block_cipher = None


a = Analysis(['fdagui.py'],
             pathex=['c:\\Users\\student\\Documents\\UVA\\6th\\CS 3240\\Fintech\\cs3240-s17-team31\\fintech\\file-dowload-application-script'],
             binaries=[],
             datas=[],
             hiddenimports=[],
             hookspath=[],
             runtime_hooks=[],
             excludes=[],
             win_no_prefer_redirects=False,
             win_private_assemblies=False,
             cipher=block_cipher)
pyz = PYZ(a.pure, a.zipped_data,
             cipher=block_cipher)
exe = EXE(pyz,
          a.scripts,
          exclude_binaries=True,
          name='fdagui',
          debug=False,
          strip=False,
          upx=True,
          console=True )
coll = COLLECT(exe,
               a.binaries,
               a.zipfiles,
               a.datas,
               strip=False,
               upx=True,
               name='fdagui')
